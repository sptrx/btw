# Keep LF in git; avoid CRLF-only diffs on GitHub (especially on Windows).
* text=auto eol=lf

# Binary assets — never line-ending normalize
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
